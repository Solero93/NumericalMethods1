#include<stdio.h>
#include<stdlib.h>
#include<math.h>

/*
Método de Romberg:
- Método de los trapezios
	1. Dividir el intervalo [a,b] en 2**i partes iguales en cada paso i
	2. Aplicar la fórmula de los trapezios
	3. Parar cuando se llege a una precisión o se evalue num veces.
- Método de extrapolación de Richardson:
	1. 
*/

double i(double args);

int main(void){

	x = malloc(sizeof(double)*(n));

	free(x);	
}
